\\",
    )
    must_sub(
        "scripts/test-desktop-fs-scope.mjs",
        "/Users/you/nexus-soak-100k",
        "/vault/nexus-soak-100k",
    )
    must_sub(
        "scripts/test-disk-fts-fill-run.mts",
        "C:\\\\\\\\Users\\\\\\\\samd1\\\\\\\\",
        "C:\\\\\\\\vault\\\\\\\\",
    )
    must_sub(
        "DESKTOP.md",
        "/Users/you/Documents/nexus-soak-100k",
        "~/Documents/nexus-soak-100k",
    )
    must_sub(
        "docs/SCALE_SOAK_REPORT.md",
        "/Users/you/Documents/nexus-soak-100k",
        "~/Documents/nexus-soak-100k",
    )
    must_sub(
        "docs/SCALE_SOAK_REPORT.md",
        "A home-dir path such as `C:\\Users\\you\\nexus-soak-100k` is still valid: programmatic open grants that folder on persisted-scope before scan.",
        "A folder outside Documents is still valid: programmatic open grants that folder on persisted-scope before scan.",
    )
    must_sub(
        "docs/SCALE_SOAK_REPORT.md",
        "That vault was written by `/workspace/gen-soak-vault.mjs` (not in this repo)",
        "That vault was written by an unofficial generator outside this repo",
    )
    must_sub(
        "docs/SCALE_SOAK_REPORT.md",
        "| Vault | `/workspace/nexus-soak-100k` (100k `.md`) |",
        "| Vault | local soak folder outside the repo (100k `.md`) |",
    )
    must_sub(
        "docs/SCALE_SOAK_REPORT.md",
        "for `/workspace/nexus-soak-100k`.",
        "for a soak folder outside the repo.",
    )
    must_sub(
        "docs/SCALE_SOAK_REPORT.md",
        "| One-off `/workspace/nexus-soak-100k` on the Grok box | `/workspace/gen-soak-vault.mjs` (**not in repo**);",
        "| One-off soak folder outside the repo | unofficial generator (**not in repo**);",
 