[package]
name = "nexus-fill-test"
version = "0.1.0"
edition = "2021"
publish = false
description = "GTK-free tests for phased / incremental SQLite FTS fill"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
